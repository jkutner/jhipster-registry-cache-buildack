#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

# fail fast
set -e

BUILD_DIR=$1
CACHE_DIR=$2

curl --retry 3 -s -L https://lang-jvm.s3.amazonaws.com/jhipster/jhipster-cache-primer.tgz | tar xzm -C $CACHE_DIR
